import SectionHeader from '@/components/SectionHeader'

const EmailGenerationDisplay = ({ original }) => {
  return (
    <div className="mt-12">
      <SectionHeader
        title="메일 생성 및 수정"
        description="답변 메일을 생성하고, 수정할 수 있습니다."
      />
      <section className="grid grid-cols-2 gap-12 rounded-xl border border-gray-200 bg-white px-6 pb-8 pt-6 drop-shadow-xs">
        text
      </section>
    </div>
  )
}

export default EmailGenerationDisplay
